let Letter = require("./Letter")

let Word = function(arr) {
    //An array of new letter objects represetnignt he letters of the underlying word

    //function that returns a string representing the word
        //this function should call the function on each letter object from Letter that displays the character or an underscore and concatenate those together (function 1 in letter.js)

    //function that takes a character as an argument and calls the guess function in each leather object (function 2 in letter.js)
}

module.exports = Word;